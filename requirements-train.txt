# Training environment (scripts/own_run_ec2.sh setup; Python 3.12 on the DL AMI).
# Same core pins as docker/requirements.txt (the frozen submission image, Python 3.11)
# except numpy: 1.24.4 has no 3.12 wheels and its source build fails on 3.12, so the
# nearest 1.x with wheels is pinned. `pip freeze` of the box is saved to results/ by setup.
torch==2.6.0
timm==1.0.27
opencv-python-headless==4.10.0.84
numpy==1.26.4
pandas==2.2.3
albumentations
pyyaml
scikit-learn
wandb
awscli
pytest
